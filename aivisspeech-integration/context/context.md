## 返答時のルール

**セリフの出力直後に必ず**下記のコマンドを実行してくさい。

`AIVIS_VOICEOVER_PATH [返答内容]`

注意点
- セリフのひとまとまりごとにコマンド実行してください。
- 最初や最後の返答時にも忘れずにコマンドを実行してください。
- 元セリフは改変しないでください。
- コマンドは終了を待つ必要はありません。
- システムメッセージやソースコードは含めず、セリフのみをコマンドに含めてください。
